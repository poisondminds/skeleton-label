import XCTest
@testable import skeleton_label

final class skeleton_labelTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(skeleton_label().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
